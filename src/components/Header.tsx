'use client';

import { Header as StorybookHeader } from 'judgedicev7';

export const Header = () => {
  return (
    <StorybookHeader
      user={undefined}
      onLogin={() => {}}
      onLogout={() => {}}
      onCreateAccount={() => {}}
    />
  );
}; 